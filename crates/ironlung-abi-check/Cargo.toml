[package]
name = "ironlung-abi-check"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "ironlung-abi-check"
path = "src/main.rs"

[dependencies]
libc = { version = "0.2", default-features = false, optional = true }

[features]
default = []
layout-check = ["libc"]
